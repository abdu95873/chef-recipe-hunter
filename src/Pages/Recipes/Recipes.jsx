import React from 'react';

const Recipes = ({recipes}) => {
   const {name} = recipes;

    
    return (
        <div>
            <h1>this is recipers: {recipe1.ingredient}</h1>
        </div>
    );
};

export default Recipes;